# C-003b: LLM 参数三级回退 + CA_LLM_THINK 可配置
> **⚠️ 历史存档 — v4.4 C-stage 降级链设计。`_call_llm_for_l1`/`_run_c_stage` 已在 v5.10 移除，由 F-stage `_run_f_stage`/`_call_llm_for_fct` 替代。**


---
**父节点:** [[C-003]]
---

**Parent:** C-003

**Context:** `think` 硬编码导致 Qwen3 无法关闭 reasoning 层; 升级后需灵活性

**Decision:** LLM 参数注入链: 实例属性 → Config 类属性 → 硬编码默认值. `think` 模式由 `CA_LLM_THINK` 环境变量控制 (None/True/False)

**Evidence:** ca/__init__.py L789 (`if Config.LLM_THINK is not None`); ca/config.py L55-L67; review fix commit 6a8d601
