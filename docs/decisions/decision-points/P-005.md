# P-005: 工具轮缓冲池 - ToolBuffer (PR2)
> **⚠️ 历史存档 — v4.7 ToolBuffer 过渡方案。v5.10 E-stage 用 3 钩子实时写即落盘，无需中间 buffer。**


---
**父节点:** [[P-001]]
---

**Parent:** P-001

**Context:** 替代旧方案从 messages 列表 `_extract_tool_calls()` 滞后拆包 → 更精确的数据采集

**Decision:** `post_api_request` 创建 `ToolGroupBuffer` → `pre_tool_call` 注册占位 → `post_tool_call` 填充结果 → `flush_tool_buffer()` 在 post_llm_call 写入 store

**Evidence:** tester plugin `__init__.py`; tester AGENTS.md
