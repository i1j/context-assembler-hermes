# SC-001: 主键从 `(turn_type, sub_index)` → `(api_call_count, seq_index)`

---
**父节点:** [[R-000]]
---

**Parent:** R-000

**Context:** 旧 `(turn_type, tool_sub_index)` 无法表达工具组关系, l2_text JSON 捆多条消息
- 旧: 1 DB 记录 ≈ 1 工具调用, 20.4× 重复写入
- 新: 每行 = 1 条 JSONL 消息, 独立列存储

**Decision:** turn_cache 主键改为 `(session_id, turn_index, api_call_count, seq_index)`

**Evidence:** tester profile AGENTS.md (turn_cache 表定义); tool-turn-refactor-analysis.md §二; tool-turn-refactor-technical-plan.md v5.0-r2
