# D-006: Executor shutdown 防竞态 (2026-06-05)

---
**父节点:** [[D-001]]
---

**Parent:** D-001

**Context:** `_shutdown_cache_executor()` 没设 `_destroyed` → 引擎销毁后重建仍提交任务

**Evidence:** changelog v4.4.1 bug fixes

**Fix:** `destroy()` 和 `reset()` 改为调用 `cache.destroy()` (设 destroyed+cancel retry+shutdown executor)
