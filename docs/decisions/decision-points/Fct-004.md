# Fct-004: parse_v1_markdown_xml() — 新防御性解析器

---
**父节点:** [[Fct-001]]
---

**Parent:** Fct-001

**Decision:** 新增 `parse_v1_markdown_xml()` 替代旧 `ooda_parser.parse()` 路径; 直接解析 Markdown+XML 输出为结构化 l1_dict

**Evidence:** ca/post_process.py (tester profile); ca-l1-refactor/ca-l1-refactor-impl-plan-v2.md §4.1

**Detail:** `ooda_parser.py` 已于 2026-09-02 删除。`parse_v1_markdown_xml()` 直接解析，标题别名来自 `post_process.py` 的 `TITLE_MAP`

**Flow:** 物理截断尾随噪音(找到 `</core_change>` 后切断) → 提取 `<core_change>` → 提取 4 类 OODA Markdown 列表 → 语义短路 → (l1_dict, l0_text)
