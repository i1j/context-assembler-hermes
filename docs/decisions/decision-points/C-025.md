# C-025: should_compress() = False (always assemble)

---
**父节点:** [[C-010]]
---

**Parent:** C-010

**Context:** 历史遗留返回 `True` → 主循环每轮打印 "Compacting context", 虽 compress() 是空操作

**Decision:** `should_compress()` 固定返回 `False`, 汇编仅由 hook `pre_llm_call` 驱动

**Evidence:** plugins/ca_assembler/__init__.py; development-report.md (2026-05-24); SRS (always assemble design)
