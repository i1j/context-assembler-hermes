# C-002: turn_index = max(counter+1, history_user_msg_count)

---
**父节点:** [[C-001]]
---

**Parent:** C-001

**Context:** 异步写入延迟 → DB `count()` 滞后 → 再次启动时重复覆盖

**Decision:** `target = max(self._turn_counter + 1, len([m for m in history if role==\"user\"]))` 锁内计算

**Evidence:** ca/__init__.py L207-L213; changelog v4.3 (memory counter设计), v4.3.2-incident-report 缺陷#3

**Related:** 启动时 `_restore_turn_index()` 从 DB max_turn_index() 恢复 (L162-L171)
