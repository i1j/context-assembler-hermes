# V-001: Replace/Append/Off 三路注入模式

---
**父节点:** [[R-000]]
---

**Parent:** R-000

**Context:** v4.4 只有 mutation 一条路径, 无法灵活切换

**Decision:** `CA_HISTORY_INJECTION` 控制: replace(默认) 1:1 对齐替换+移除行; append 拼接标签文本; off 不注入只积累

**Evidence:** tester plugin AGENTS.md; tester plugin `__init__.py` (`_build_aligned_outcomes` / `_build_messages_from_plan`)
