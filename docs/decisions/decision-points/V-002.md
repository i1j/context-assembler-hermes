# V-002: Replace 模式 = 行类型注射表
> **⚠️ 历史存档 — v5.1 旧设计 `_build_aligned_outcomes`。v5.10 改用 `_simple_mutation_mode_v5` (plugin __init__.py)。**


---
**父节点:** [[V-001]]
---

**Parent:** V-001

**Decision:** `_build_aligned_outcomes()` 输出表:
user → None(保留) / Fct文本 / Hdl文本
assistant{tc} → None(保留) / `_format_tool_group_assembly()` / 紧凑格式
tool → `""` (全部删除, 信息合并到 assistant{tc})
assistant_fin → None

**Evidence:** tester AGENTS.md; design/replace-mode-injection-refactoring.md (已实装)
