# C-014a: `_select_upgrades` — RRF 得分排序 (审查修正)

---
**父节点:** [[C-014]]
---

**Parent:** C-014

**Context:** code-review-v4.4.0 发现 SRS(按RRF得分) vs 设计文档(按节省量) 自相矛盾, 代码跟随设计文档

**Decision:** 同类型内按 RRF 得分降序排列, Token 节省量仅用于预算准入判断

**Evidence:** ca/__init__.py L594-L605; code-review-v4.4.0.md §1.2; SRS REQ-TOOL-P005

**Fix:** `sort(key=lambda c: (0 if type==\"dialogue\" else 1, -c[\"rrf_score\"]))`, 节省量仅 `if c[\"saving\"] <= 0: continue` 和 `if remaining <= 0: break`
