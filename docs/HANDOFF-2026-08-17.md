# CA Assembler Hermes 版 Handoff（2026-08-17：决策 44 E/F/strand 改造 + 决策 45 v3 记录）

> 跨会话交接单一入口。下个会话先读：
> 1. 本文件（当前状态与下一步）
> 2. `docs/decisions/44-e-stage-granularity-think/44-e-stage-granularity-think.md`（需求/方案/接口契约）
> 3. `docs/decisions/44-e-stage-granularity-think/test-requirements.md`（测试契约）
> 4. `docs/decisions/45-multi-affair-ooda-record/45-multi-affair-ooda-record.md`（v3 记录契约）
> 5. `docs/architecture/03-e-stage/03-e-stage.md` + `docs/architecture/04-f-stage/04-f-stage.md`
> 6. DSH 参照：`~/dsh-workspace/ca-v7/docs/CA-V7-7.1-tool信息搜集与处理设计.md`、
>    `CA-V7-7.2-本地知识层设计.md`、`~/docs/eflr.md`

## 1. 项目与目标

- 项目：`/home/i1j/.hermes/profiles/tester/plugins/ca_assembler`（git: gitee elite1j/context-assembler-plugin）。
- 本次任务：参考 DSH 版 ca-v7 与 EFLR（`~/projects/courier3/eflr`）改进 Hermes 版 E 阶段：
  近源采集、细颗粒度块模型与元数据、云端 think 录入，支撑 Fct 多事务 OODA 摘要。
- 用户裁定（本会话，按时间）：
  1. 提问后首轮 think 是事务划分关键 → 新增 `orient` 卡，零门槛入卡；
  2. Fct 输入注入代码筛选的 think 卡，输出多事务 `affairs[]` OODA；
  3. **效率优先，不向后兼容**：strand 部分直接消费 `affairs`，恢复 flash 式
     `1. xxx / 2. yyy` 编号事务清单（这些就是事务 hdl）；
  4. **多事务 OODA 记录单一化（决策 45）**：多事务模式去掉 changes 与
     【已完成】等状态标签——事务必处 OODA 某环节，记录只需
     `事务 hdl + OODA 阶段关键字 → 该阶段变更数组`（v3 契约）。

## 2. 当前状态（已验证）

- `python -m pytest -q -c tests/pytest.ini tests`：**1161 passed / 1 skipped / 1 xfailed**。
- `python -m py_compile` 关键文件全过；`register(ctx)` 冒烟 = **13 hooks**。
- 工作区已提交，代码 HEAD = `0803887`（决策 45 单一数据源收尾 + TP-001 元数据修复），无未提交改动。
- **多 profile 同步完成**：`scripts/sync-ca.sh winker` + `sync-ca.sh sysadmin` 已跑，
  winker/sysadmin 运行时文件与 tester 一致（diff 无差异、py_compile OK）。
- **已发布**：context-assembler-plugin `v6.0` 推送 Gitee（`65cf649`）；
  profiles 仓库 `master` 推送 Gitee（`210d43f`，含 tester/winker/sysadmin 插件同步）。
- 发测状态机：`wf-state.json` current_state = 交付部署，exit_checks.last = PASS。

### commit 链（本次会话，均可查）

| commit | 内容 |
|--------|------|
| `049f3f0` | E 改造前基线固化（CE 壳审计交付未提交改动） |
| `0c8fa2c` | 决策 44 主体：13 hooks、turn_stream v7 列、llm_calls/think_trace、blocks/meta_marker/think_collect、Fct 事务帧 |
| `151c868` | 发测状态机登记 |
| `c2cd1cb` | 首轮 think `orient` 卡零门槛 + reasoning 全字段提取 |
| `63131b9` | 状态机补登记 |
| `46288c5` | 澄清 DSH 卡规则 vs L2 原文保留口径 |
| `fa0a65a` | Fct 多事务 OODA 输出 + 代码筛选 think 卡输入 |
| `b086ac2` | strand 直接消费 Fct affairs（编号候选 hdl，不向后兼容） |
| `431e704` | 决策 45：多事务 OODA 记录 v3 单一化（去掉 changes/状态标签） |
| `04253a9` | 决策 45 收尾：单一数据源全链路（删 legacy 扁平字段、fallback 用 affairs 聚合 strands） |
| `0803887` | `_fct_format` 加入 Fct 元数据剥离键集（TP-001 同源） |

## 3. 已实现的关键机制

### 3.1 E 阶段（近源采集）
- hooks 8→13：新增 `pre_api_request` / `api_request_error` / `on_stream_start` /
  `on_stream_delta` / `on_stream_end`（`__init__.py::register`）。
- `post_api_request` 为**每次 API 调用**级主写点（比 turn 级 post_llm_call 更近源）：
  - 拆块写 `turn_stream`：`THINKING`（decide）/ `AGENT_REPLY`（decide）/
    `tool_call_request`（act）/ `tool_call_result`（observe）；
  - 每行带 `request_id/provider/model/reasoning_chars/text_chars/result_chars/
    error_text/is_fin/metadata_incomplete/block_type/ooda_stage`。
- 流式旁路：`on_stream_*` 只做内存计数（`plugin._stream_pending`），
  `on_stream_end` 一次性 UPSERT 补丁 `llm_calls`（统计列 MAX 合并）；
  与 `post_api_request` 无顺序保证，但 UPSERT 保证不丢。
- 注意：`plugins.stream_reasoning_deltas` 默认 false 只影响流式 delta；
  reasoning 全量仍从 `post_api_request.assistant_message` 提取（`ca/meta_marker.py`）。

### 3.2 存储（`ca/store.py`）
- turn_stream v7 增量列：旧库 `_migrate_turn_stream_v7` 幂等 ALTER（user_version=1）。
- 新表：
  - `llm_calls`：`UNIQUE(session_id, request_id)`，retry latest-wins；usage 全量 JSON、
    finish_kind/duration/failure、stream chunk 计数。
  - `think_trace`：`UNIQUE(session_id, turn, seq)`，只存 `raw_len` 指针 + preview≤160；
    reasoning 全文留在 turn_stream THINKING 行（L2）。
- `write_turn_v5` 核心列比较已扩展到 v7 新列（旧行 NULL 补齐），
  重放仍保护已回填的 Fct/Hdl。

### 3.3 think（`ca/think_collect.py` + `ca/meta_marker.py`）
- 卡类型：`decision`（reasoning+tool_calls，任意长度）；
  `orient`（事务内首段无工具 think，**零门槛**）；`conclusion`（fin 轮 ≥800 字/
  修正词表/工具错误）。
- reasoning 提取顺序：`provider_data.reasoning_content` → 顶层 `.reasoning` →
  `reasoning_details`/`codex_reasoning_items`/`codex_message_items`/
  `anthropic_content_blocks` → 内联 `<think>` 标签兜底；双源去重合并，不把
  content 冒充 reasoning。
- 历史数据边界：v6.1 之前纯对话首轮 think 未落盘，**不可恢复**；旧 thought 行
  （工具轮）可回填 decision 卡（脚本待写）。

### 3.4 Fct（`ca/fct_multi_affair.py` + `ca/f_stage.py` + `ca/prompts.py`）
- 输入：事务帧 `[ooda_stage|block_type]` + 代码筛选 think 卡
  （当前 turn、orient 优先、max 2 卡、每卡 800 字、总预算 1600）。
- 输出（**v3，决策 45**）：`FCT_GENERATION_PROMPT_MULTI_AFFAIR` 要求
  `{"affairs":[{hdl,turns,ooda{四段}}]}`——OODA 四段数组项即该阶段变更，
  **无 changes / stage_tag / 【已完成】等标签**；`_fct_format=v3-multi-affair-ooda`。
- **落库单一数据源**：`build_fct_multi_affair` 只写
  `affairs + _assemble_status + _fct_format`，不再写任何 legacy 扁平字段。
- v2 旧库只读兼容：解析仍接受带 `changes[{stage_tag,core_change}]` 的记录。
- 解析：`parse_fct_multi_affair`（剥围栏/JSON 对象/逐字段校验）→
  `build_fct_multi_affair` 落库；旧消费者视图由读路径现场派生。
- 降级链：multi-affair JSON → legacy `parse_v1_markdown_xml` →
  截断 partial JSON → `PAIR_PATTERN` → pending backfill。
- 开关：`CA_FCT_MULTI_AFFAIR_ENABLED=1`（0 整体回退旧 prompt/解析）。

### 3.5 Strand（`ca/store.py::collect_turn_fcts` + `ca/topic_summary.py`）
- `collect_turn_fcts` 以 Fct `affairs[]` 为唯一内容源（v3：hdl/turns/ooda；
  v2：另含 changes），**现场派生**旧消费者视图 changes/tags/ooda_tags/
  todos/consensus/key_facts_supp/new_materials；无 affairs 的旧 Fct 走原解析。
- `_format_turns_for_prompt`：有 affairs 时渲染
  `事务清单（编号即候选 strand）：1. hdl / 2. hdl` + 每事务 OODA 阶段项
  （阶段项即变更，无状态标签）；仅 v2 旧记录额外渲染 `### changes（旧版兼容）`；
  有 affairs 即跳过 legacy changes 重复输入（效率优先）。
- **4B 失败兜底**：`_fallback_strands_from_affairs` 按同名事务跨轮合并出
  多 strands（turns 并集、OODA 四段去重 concat），不再单 strand 打包；
  `_compute_status` 改为「任一事务有后续行动 → active」。
- `TOPIC_SUMMARIZE_PROMPT` 新增规则：编号即候选 strand，优先跨轮合并、
  不凭空发明/重命名；只有编号内明显含两条工作线才再拆；阶段项即变更，
  禁止给它们发明【已实施】等标签。
- `topic_manager._extract_fct_semantic_text`：affairs 上下文内的 ooda 值
  是语义内容（保留）；legacy changes 的 ooda 标签仍剥除。

## 4. 下个会话建议（按优先级）

1. **think L1 提炼**：`think_trace.status=raw → l1`，移植 DSH 7.2 K1
   （每卡最多 1 次本地 4B、失败保持 raw、fail-open）；输入 question + reasoning
   截断 6000 + 工具摘要≤8 条；flash 版本必须 `thinking:{type:'disabled'}`。
2. **历史回填脚本**：扫描 per-session DB，把旧 `role=assistant, finish_reason != stop,
   Elm 非空` 的 thought 行回填 decision 卡（首段无工具行回填 orient）；写清楚
   v6.1 前纯对话首轮 think 无法恢复。
3. **真库抽检**：历史会话回放验证 affairs 多事务拆分质量（单轮多事务 split 稳定性）；
   若 4B 把 affairs 写碎，在 `parse_fct_multi_affair`/`build_fct_multi_affair`
   之间加确定性近重复事务合并；并验证 v3-only Fct 的 embedding 语义文本、
   topic fallback strands 与历史摘要质量。
4. 后续（不阻塞）：think 召回注入（K3）、reasoning wire 压缩（K4，先入卡后替换）。

## 5. 关键文件与测试

| 文件 | 职责 |
|------|------|
| `ca/blocks.py` | BlockType / ooda_stage 映射 / 事务帧格式化（零 LLM） |
| `ca/meta_marker.py` | provider 推断、usage 归一化、reasoning 全字段提取、API 元数据 |
| `ca/think_collect.py` | think 卡门槛（decision/orient/conclusion）、preview、指针 |
| `ca/fct_multi_affair.py` | affairs v3 解析 / build_fct_multi_affair 单一数据源落库 / v2 只读、think 卡筛选 |
| `ca/e_stage.py` | `_on_api_response_v7` / `_on_final_response_v7` / error hook |
| `ca/f_stage.py` | 事务帧 + think 输入 + multi-affair v3 解析优先 + hdl 事务名优先 |
| `ca/topic_summary.py` | 编号候选 strand 输入、affairs 聚合兜底 strands、status 阶段化 |
| `ca/store.py` | v7 迁移、llm_calls/think_trace、collect_turn_fcts 从 affairs 派生旧视图、v3 历史摘要 |
| `ca/a_stage.py` | `_format_fct_readable` 多事务 hdl+阶段渲染 |
| `topic_manager.py` | Fct 语义文本提取：affairs 内 ooda 值放行、legacy 标签剥除 |
| `__init__.py` | 13 hooks 注册、stream pending、post_api/post_llm 派发 |
| `plugin.yaml` | hooks 13、config_schema：stream/fct_structured/fct_multi_affair/think |

测试：`tests/unit/{test_blocks,test_meta_marker,test_think_collect,test_fct_multi_affair,test_strand_affair_input,test_topic_summary,test_topic_manager}.py`、
`tests/store/{test_store_v7_meta,test_store_contract}.py`、
`tests/stage/{test_e_stage_v7,test_f_stage_v7_frames,test_f_stage}.py`（v3 单一数据源/display/hdl/validity）、
`tests/plugin/test_plugin.py`（hook 计数 13）、`tests/unit/test_ce_shell.py`（hook 列表）。

运行：`cd /home/i1j/.hermes/profiles/tester/plugins/ca_assembler && python -m pytest -q -c tests/pytest.ini tests`

## 6. 风险与已知坑

- `on_stream_*` 异步 worker 与 `post_api_request` 同步 invoke **无顺序保证**——
  一律走 `llm_calls` UPSERT，不要改成纯 UPDATE。
- stream 回调必须 `**kwargs`（Hermes worker 不做签名裁剪）。
- `post_api_request.usage` 无 `completion_tokens`：canonical summary 用
  `output_tokens`；turn_stream 旧列映射 `usage_completion_tokens=output_tokens`。
- reasoning 原文只落 per-session DB（turn_stream THINKING 行），不复制进共享
  DB/OV；preview≤160。
- Fct v3 已单一数据源（只存 affairs + 装配元数据）；旧消费者视图全部在
  `collect_turn_fcts` 读路径现场派生，不要再往 Fct JSON 加回 legacy 扁平字段。
- `_extract_fct_semantic_text` 的 `ooda` 放行只在 `affairs[]` 上下文内生效；
  legacy changes 的 `ooda` 仍是元数据标签，改这段时注意别把标签值灌进 embedding。
- 旧库迁移幂等但只补列：`PRAGMA user_version` 已置 1，新表在 per-session DB，
  未来迁移不要动旧 11 表语义。
